#pragma once

#include <QObject>
#include "ClientDataTypes.h"

/**
 * @brief Sprite data management
 * 
 * Placeholder implementation - will be fully implemented in later tasks
 */
class SpriteDataManager : public QObject
{
    Q_OBJECT

public:
    explicit SpriteDataManager(QObject* parent = nullptr);
    ~SpriteDataManager();

private:
    // Implementation will be added in later tasks
};